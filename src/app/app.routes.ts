import { Routes } from '@angular/router';
import { AppComponent } from './app.component'; // Si es necesario importar el componente

export const routes: Routes = [
  {
    path: '',
    component: AppComponent, // Asegúrate de que la ruta esté bien definida
  },
];
